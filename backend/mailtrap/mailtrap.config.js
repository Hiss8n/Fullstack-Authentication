/* const Nodemailer = require("nodemailer");
const { MailtrapTransport } = require("mailtrap");


 const mailtrapClient = Nodemailer.createTransport(
  MailtrapTransport({
    token:process.env.MAILTRAP_TOKEN,
    
  })
);



const sender = {
  address: "hello@example.com",
  name: "Tutu Suleiman",
};  */




/* 

const recipients = [
  "hissentutu4@gmail.com",
];

transport
  .sendMail({
    from: sender,
    to: recipients,
    subject: "Notification",
    text: "Congratulations on your new journey!",
    category: "I sm sending your this email to let you know that , You successfully subscribed to the $ 5000",
  })
  .then(console.log, console.error); */


/* module.exports={mailtrapClient,sender} */